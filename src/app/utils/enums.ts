import { Patient } from '../pages/patients/patient.model';
export enum validRoles {
  Admin = 1,
  Professional,
  Patient,
}

export enum validDays {
  Lunes = 1,
  Martes,
  Miercoles,
  Jueves,
  Viernes,
  Sabado,
  Domingo
}
